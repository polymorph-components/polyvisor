[package]
name = "polyvisor-todomvc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

# M0: no dependencies yet — M1 tracks add them.
