# Cargo build output for any crate in the tree (unanchored: matches
# nested crate target/ dirs under engine/, examples/, providers/, etc.,
# not just a repo-root target/).
target/

# The demo site under docs/demo/ is BUILT by CI on every push
# (.github/workflows/pages.yml) and deployed via the GitHub Actions Pages
# source (flipped 2026-08-18) — it is generated, not committed. Before
# the cutover, four rebuilds of an ~11 MB engine composite landed in
# history. `just pages` still writes here for local preview.
/docs/demo/
# Same class: spikes/todomvc's justfile writes its built demo here.
/docs/spike-todomvc/
