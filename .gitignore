# Unanchored: nested crates (runtime/component, visor, apps/*) each grow
# their own target/ too.
target/
node_modules/
web/dist/
.playwright/
# Wasm build output only; *.wasm elsewhere (e.g. checked-in test fixtures)
# is not ignored.
web/dist/*.wasm
