[package]
name = "polyvisor-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
# cdylib: builds to a wasm component; rlib: lets other workspace crates and
# tests depend on it as a normal Rust lib.
crate-type = ["cdylib", "rlib"]

# M0: no dependencies yet — M1 tracks add them.
