[toolchain]
# docs/design.md "Pins, with reasons": current stable; satisfies stream-dom
# (1.98), subduction (1.91), keyhive (1.90).
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = ["wasm32-wasip2"]
